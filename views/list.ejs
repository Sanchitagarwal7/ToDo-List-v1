<%- include('header'); -%>

        <!-- <% if(ListTitle == 'Tuesday' || ListTitle== 'Saturday'){ %>
            <h1 style="color: red">It's a <%= ListTitle %></h1>
        <% }else{ %>
            <h1 style="color: blue">It's a <%= ListTitle %></h1>
        <%}%> -->

        <div class="box" id="heading">
            <h1><%= ListTitle %></h1>
        </div>

        <div class="box">
                <% for(let i = 0; i<newListitems.length; i++){ %>
                    <div class="item">
                        <input type="checkbox">
                        <p> <%= newListitems[i] %> </p>
                   </div>
                <% } %>

                <form action="/" method="post">  
                    <label for="newItems"></label>
                    <input type="text" id="item" name="new" placeholder="New Item" autocomplete="off">
                    <button type="submit" name="button" value=<%= ListTitle %> id="button">+</button>
                </form>
        </div>
        <form action="/about" method="get">
            <button type="submit" id="abt" > About me </button>
        </form>
        <br>

<%- include('footer'); -%>